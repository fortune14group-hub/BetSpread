import React from 'react'; import { Bet } from '../lib'; export default function BetTable({bets,onSet}:{bets:Bet[],onSet:(id:string,r:Bet['resultat'])=>void}){return(<div className="card overflow-auto"><table><thead><tr><th>#</th><th>Utskick</th><th>Matchdag</th><th>Match</th><th>Markets</th><th>Odds</th><th>Units</th><th>Resultat</th><th>Actions</th></tr></thead><tbody>{bets.map((b,i)=>(<tr key={b.id}><td>{i+1}</td><td>{b.utskick}</td><td>{b.matchdag}</td><td>{b.match}</td><td>{b.market}</td><td>{b.odds.toFixed(2)}</td><td>{b.unitsize.toFixed(2)}</td><td>{b.resultat==='SITTER'&&<span className="badge badge-green">Sitter</span>} {b.resultat==='SITTER_EJ'&&<span className="badge badge-red">Sitter ej</span>} {b.resultat==='VOID'&&<span className="badge badge-yellow">Void</span>} {b.resultat==='PENDING'&&<span className="text-neutral-400">Pending</span>}</td><td className="space-x-2"><button className="btn-outline" onClick={()=>onSet(b.id,'SITTER')}>Sitter</button><button className="btn-outline" onClick={()=>onSet(b.id,'SITTER_EJ')}>Sitter ej</button><button className="btn-outline" onClick={()=>onSet(b.id,'VOID')}>Void</button></td></tr>))}</tbody></table></div>)}